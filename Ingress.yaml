apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-fanout-example
spec:
  rules:
  - host: app.com
    http:
      paths:
      - path: /spring-boot-test1
        pathType: Prefix
        backend:
          service:
            name: service1
            port:
              number: 8080
